spring:
  application:
    name: planar

  datasource:
    url: jdbc:postgresql://${PLANAR_DB_HOST}:${PLANAR_DB_PORT}/${PLANAR_DB_NAME}
    username: ${PLANAR_DB_USERNAME}
    password: ${PLANAR_DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  h2:
    console:
      enabled: true
      path: /h2-console

  mail:
    host: ${SMTP_HOST:smtp.gmail.com}
    port: ${SMTP_PORT:587}
    username: ${SMTP_USERNAME:}
    password: ${SMTP_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

  jackson:
    serialization:
      write-dates-as-timestamps: false

# Railway injects PORT and routes the public URL to it; fall back to 8080 locally.
server:
  port: ${PORT:8080}

planar:
  rate-limiter:
    capacity: 60
    refill-rate: 10
    refill-interval-seconds: 1